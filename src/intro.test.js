// import sum from './sum'

// TDD


// write a function that takes in 2 numbers and returns the sum

// describe('Testing the sum module', () => {
// // pass in 2 numbers (1 and 1), expect t2 as the sum
// test('sum of 1 and 1', () => {
// expect(sum(1,1)).toBe(2);
// })
// // pass - 1 and 1, expect 0
// test('sum of -1 and 1', () => {
//     expect(sum(1,-1)).toBe(0);
// })
// // .5 and 1, expect 1.5
// test('sum of .5 and 1', () => {
//     expect(sum(.5,1)).toBe(1.5);
//     })
// // '1' (string of 1) and 2, expect 3
// test('sum of strings', () => {
//     expect(sum('1', 2)).toBe(3);
//     })
// // 1, expect 1
// test('sum of 1 number', () => {
//     expect(sum(1, )).toBe(1);
//     })
// // 'ten' 1, expect NaN
// test('sum of string and number', () => {
//     expect(sum('ten', 1)).toBe(NaN);
//     })


// })
